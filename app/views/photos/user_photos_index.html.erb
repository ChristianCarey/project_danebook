<%= render 'shared/banner', user: @user %>
<section id="profile-content">
  <header id="profile-header">
    <h1>Photos</h1>
  </header>
  <div id="profile-content-body" class="container">
    <% present_collection @photos do |photos_presenter| %>
      <%= photos_presenter.in_rows_of(4) %>
    <% end %>
  </div>
</section>
