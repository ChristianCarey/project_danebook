(function() {
  console.log('wat');
  var $commentableFormContainer = $('[data-commentable-id="<%= @commentable.id%>"][data-commentable-type="<%= @commentable.class.to_s.downcase %>"]');
  var $errors = $commentableFormContainer.find('.comment-errors');
  $errors.text("Comment cannot be blank")
  $errors.css({
    color: "red"
  });
  $commentableFormContainer.find('textarea').css({
    border: "1px solid red"
  });
  $commentableFormContainer.find('.comment-input-actions').prepend($errors);
})();